{
  "name": "rashinban_lp",
  "version": "1.0.0",
  "description": "Simple static webpage with HTML/LESS/JS",
  "main": "dist/index.html",
  "scripts": {
    "build": "node build-tools/compile.js",
    "serve": "http-server dist -p 8080 -o",
    "dev": "npm run build && npm run serve"
  },
  "dependencies": {
    "http-server": "^14.1.1"
  },
  "devDependencies": {
    "less": "^4.2.0"
  }
}
